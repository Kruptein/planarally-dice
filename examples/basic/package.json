{
    "name": "babylonjs-dice-webpack",
    "version": "1.0.0",
    "description": "An example project with Webpack",
    "private": true,
    "scripts": {
        "build": "rimraf dist && tsc"
    },
    "devDependencies": {
        "rimraf": "^3.0.2",
        "ts-loader": "^8.0.18",
        "typescript": "^4.2.3"
    },
    "dependencies": {
        "@babylonjs/core": "^4.2.0",
        "@planarally/dice": "^0.2.0"
    }
}
